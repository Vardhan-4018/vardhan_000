#include<stdio.h>
int main()
{
    int arr[]={1,2,3,4};
    int n=4,K=10,i,j,prod;

    for(i=0;i<n;i++)
    {
        prod=1;
        for(j=i;j<n;j++)
        {
            prod*=arr[j];
            if(prod<K)
                printf("Subarray %d to %d\n",i,j);
        }
    }
}
